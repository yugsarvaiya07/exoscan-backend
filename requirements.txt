fastapi==0.111.0
uvicorn[standard]==0.30.1
numpy==1.26.4
scipy==1.13.1
pydantic==2.7.4

# Astronomy stack (used for real Kepler/TESS ingestion + BLS).
# The backend gracefully falls back to synthetic data / a custom BLS
# implementation if these fail to install or fail at runtime, so the
# app still works without them — but installing them gives you real data.
lightkurve==2.5.0
astropy==6.1.0
